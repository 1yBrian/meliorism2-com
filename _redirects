# Cloudflare redirect rules.
# Under Workers Static Assets, valid status codes are 200, 301, 302, 303, 307, 308 — NOT 404.
# Internal paths are blocked via .assetsignore (not uploaded at all) — no rule needed here.
# Add legitimate redirects here as needed, one per line.
